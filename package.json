{
  "name": "hackernews-prism",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build-client": "prism compile-app",
    "build": "npm run build-client && cargo build",
    "client": "npm run build-client -- --context client --run",
    "start": "npm run build-client && cargo run"
  },
  "author": "kaleidawave",
  "license": "MIT",
  "devDependencies": {
    "@kaleidawave/prism": "^1.5.0"
  }
}
